rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // TEMPORARY: Allow all read/write for development testing
    // WARNING: This is insecure! Replace with proper authentication before production
    match /{document=**} {
      allow read, write: if true;
    }

    /* Production rules - uncomment and deploy when ready for production:
    
    // Users: Can read any profile, but only update their own.
    match /users/{userId} {
      allow read: if request.auth != null;
      allow update: if request.auth != null && request.auth.uid == userId;
      allow create, delete: if false; // Managed by functions
    }

    // Events: Authenticated users can read, create, update, and delete events.
    match /events/{eventId} {
      allow read, create, update, delete: if request.auth != null;
    }

    // Stakeholders: Authenticated users can manage stakeholders.
    match /stakeholders/{stakeholderId} {
      allow read, create, update, delete: if request.auth != null;
    }

    // EventStakeholders: Authenticated users can link/unlink stakeholders.
    match /eventStakeholders/{docId} {
      allow read, create, delete: if request.auth != null;
    }

    // Notifications: Users can only read and update their own notifications.
    match /notifications/{notificationId} {
      allow read, update: if request.auth != null && request.auth.uid == resource.data.userId;
      allow create: if request.auth != null;
    }
    */
  }
}